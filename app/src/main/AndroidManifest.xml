<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.knuhack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KnuHack"
        android:usesCleartextTraffic="true">
        <activity android:name=".CommentDetail"></activity>
        <activity android:name=".CommentList"></activity>
        <activity android:name=".PostDetail" />
        <activity android:name=".write_post" />
        <activity android:name=".postList" />
        <activity android:name=".SignUp" />
        <activity android:name=".FindTeam" />
        <activity android:name=".MyPage" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>